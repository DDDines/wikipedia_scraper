# import src.utils as utils
from src.scraper import status

if __name__ == "__main__":
    """
    Main entry point of the script.yy


    This script allows you to distribute a list of people randomly on seats at tables in a room.
    By default, the system creates 4 tables with 6 seats each and uses the list from 'colleagues.txt'.
    You can customize the system's parameters or use the default configuration via the menu.
s
    The 'display_menu' function is called to start the program.
    """

    print(status())
